using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraBackToPlayer : MonoBehaviour
{
/*
    [SerializeField] private GameObject _camera;
    [SerializeField] private GameObject _cameraToRemove;
    public void await() {
    _camera.transform.position.x = -0.1599926
    _camera.transform.position.y =1.3799
    _camera.transform.position.z =1.5899

    _cameraToRemove.SetActive(false);
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
